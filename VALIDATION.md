# 验证记录

2026-09-08，在实际 Apple M4 / 16 GB 统一内存 / macOS 26.5.1 上执行。

## 已通过

- 9 项自动测试：严格请求 schema、Origin / Host 拒绝、challenge 一次性配对、session 绑定/轮换/撤销、内存与上下文规划、确定性排序、下载暂停与状态重启恢复、错误 manifest 拒绝启动。
- Bridge、共享包、脚本和测试 TypeScript 检查。
- 网页 TypeScript 检查与正式构建。
- 真实硬件探测。
- 从 Ollama 官方注册表下载 Qwen 3 Mini `qwen3:0.6b`（522,653,277 bytes）。
- 本地模型 manifest SHA-256 与注册表固定值一致：`7df6b6e09427a769808717c0a93cadc4ae99ed4eb8bf5ca557c90846becea435`。
- 部署从 checking、downloading、verifying、starting 到 running；运行状态由 Ollama `/api/ps` 验证。
- 本地非流式中文回答、OpenAI-compatible SSE 流式回答及结束标志。
- 本地基准：首次短测约 151.7 tok/s，非长期稳定性能承诺。
- 停止后 `/api/ps` 不含该模型；再次启动恢复运行。

## 验证边界

真实下载/推理集成测试使用单独的临时 Bridge 数据目录；模型由同一本机 Ollama 管理，网页可在 My AI 中发现。

暂停和断点恢复的状态机验证使用模拟的 Ollama 网络流，并未进行断网压力测试。真实完整模型下载已经通过。macOS runtime 自动安装分支实现了固定官方归档下载和 SHA-256 验证，本机已有 Ollama，因此未实际执行重新安装。

未进行浏览器自动化点击、截图或跨浏览器兼容性测试。网页已经通过类型检查、构建和 HTTP 预览响应验证。

脚手架的依赖审计报告仍包含传递依赖及框架 advisories。该版本作为绑定 loopback 的本地开发 MVP 交付；正式发行前应升级并复测脚手架依赖、完成签名/公证及安全审查。Bridge 核心依赖的 npm audit 当前为 0 项漏洞。

## 图片功能补充验证

新增 8 项测试，合计 17 项通过：

- OpenAI 多模态消息转换为 Ollama base64 图片参数，保留文本输入兼容性。
- 拒绝远程图片 URL、文件路径、不支持的格式、伪造格式签名、超限数量及体积。
- 文字模型通过直接 API 提交图片也会被拒绝。
- 保留最新 4 张图片的上下文顺序，较早缩略图继续保存在本地历史。
- 使用实际 HTTP Bridge、模拟 Ollama 验证图文转发、超过旧 256KB 限制的图片请求、流式输出。
- 使用 fake-indexeddb 验证图片对话保存、旧文字记录兼容、清空与关闭保存时的并发写入保护。

此前 17 项测试的视觉推理端使用模拟响应；0.2 版新增真实视觉测试，见下文。保留现有 Qwen 3 小模型。图片文件选择、剪贴板和拖拽路径已实现并通过网页类型检查及构建，未进行浏览器自动化操作测试。


## 0.2 模型库与真实视觉验证（2026-09-08）

- 27 个型号、15 个系列通过官方 manifest、配置 digest、GGUF 头部结构核验。原始证据随源代码交付；这不是 27 个型号全部实机推理通过的声明。
- 新增目录一致性、混合注意力 KV 计算、运行时版本比较、旧版运行时预检拒绝、已审计旧修订不能冒充新修订等测试，总计 22 项全部通过。
- 网页 TypeScript 与正式构建通过。后续启动器支持构建产物运行、复用现有 0.2 网页。
- 通过独立的本机 Ollama 0.32.7（端口 11435）实际下载、校验并启动 Qwen 3.5 0.8B（约 1.04 GB）。测试与用户正常运行的 11434 服务分开，未停止用户的模型。
- 真实 HTTP Bridge 接受标准 OpenAI 图片消息，将 256×256 红色 PNG 发送给模型。模型实际回答“红色”；同一图文输入的 SSE 流式回答及结束标志也通过。
- 测试后卸载测试模型的内存占用并关闭独立服务；保留已下载文件。
- 真实测试脚本：`scripts/smoke-vision.ts`，需先单独运行 `OLLAMA_HOST=127.0.0.1:11435 OLLAMA_NO_CLOUD=1 ollama serve`，再执行 `node --import tsx scripts/smoke-vision.ts`。它会下载约 1.04 GB；仅用于维护者验证。
- Ollama 安装来源已固定至官方 0.33.3，并保留官方 SHA-256；本机已有运行时，未执行该版本的安装分支。

最终交付时 Codex 会话权限切换为受限模式，重启 Bridge 的本机端口监听被系统拒绝（EPERM）。使用者需双击启动器在正常终端启动更新版本。此次没有浏览器截图、自动点击或跨浏览器测试；构建产物启动路径在此受限会话内未完成端口监听验证。
