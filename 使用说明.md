# CanIRunAI 0.2 使用说明

1. 解压整个 CanIRunAI 文件夹；请勿只移动启动器。
2. 确保使用 Apple Silicon Mac，已安装 Node.js 24。
3. 双击 `Start CanIRunAI.command`，等待安装依赖及构建完成。首次启动需要网络。
4. 浏览器会打开 `http://localhost:3000` 并自动配对。未配对时，输入启动终端中的 8 位配对码。
5. 在“探索模型”选择“精选推荐”，按当前电脑内存和存储挑选模型；“全部模型”可查看 27 个型号，支持搜索。
6. 部署后进入聊天。支持图片的模型会出现图片入口，可上传、粘贴或拖拽图片。Qwen 3.5 0.8B 已完成真实发图测试。

保持启动终端打开。聊天内容只在本机处理，图片与对话历史按设置保存在当前浏览器。下载模型需要网络，推理无需网络。

**更新说明**：模型目录于 2026-09-08 核验，共 15 个系列；属于精选本地目录，并非所有官方模型。显示“元数据已核验”的条目未全部在这台机器实测。需要更高 Ollama 版本时，按页面提示到官方下载页更新并重启 Ollama。

**连接排查**：检查启动终端是否仍在运行、3000 与 31415 端口是否被其他程序占用；配对码每 10 分钟更新。升级目录后请重新启动启动器。已有旧版服务时先在原终端按 Control+C 结束，再启动本版。

这是需 Node.js 的本地运行包；尚未提供签名、公证的独立 .app 安装包。详细技术说明见 README.md，验证范围见 VALIDATION.md。
